---
phase: 02-codebase-scanner
plan: 03
type: execute
wave: 3
depends_on: ["02-02"]
files_modified: []
autonomous: false
requirements: [SCAN-01, SCAN-02, SCAN-03, SCAN-04, SCAN-05, SCAN-06, SCAN-07]

must_haves:
  truths:
    - "Scanner produces real feature map from actual codebases"
    - "Feature map JSON is valid against FeatureMapSchema"
    - "Mobile features are classified as end_user audience"
    - "Dashboard features have admin role classification"
    - "Platform API context is present on relevant features"
    - "Infrastructure components are excluded from the feature map"
    - "Running scan twice on unchanged repos skips re-scanning"
    - "Feature slugs are stable across runs"
  artifacts:
    - path: ".sawyer-docs/feature-map.json"
      provides: "Merged feature map from all three codebases"
    - path: ".sawyer-docs/scan-state.json"
      provides: "Stored commit SHAs for change detection"
  key_links:
    - from: "src/commands/scan.ts"
      to: ".sawyer-docs/feature-map.json"
      via: "writeFeatureMap after successful scan"
      pattern: "writeFeatureMap"
---

<objective>
Verify the scanner works end-to-end with real codebases. Run the scan command against the actual mobile, dashboard, and platform repos. Verify the output feature map is correct, complete, and stable.

Purpose: Plans 01 and 02 built the scanner without live testing. This checkpoint confirms everything works together against real data.

Output: Verified feature-map.json and scan-state.json in .sawyer-docs/, confirmation that all SCAN requirements are met.
</objective>

<execution_context>
@/Users/terhuerne/.claude/get-shit-done/workflows/execute-plan.md
@/Users/terhuerne/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/phases/02-codebase-scanner/02-CONTEXT.md
@.planning/phases/02-codebase-scanner/02-01-SUMMARY.md
@.planning/phases/02-codebase-scanner/02-02-SUMMARY.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Run full scan against configured repos</name>
  <files></files>
  <action>
Run the scan command against the repos configured in sawyer-docs.config.json:

```bash
npx tsx src/bin/cli.ts scan
```

If repo paths in config are not yet pointing to real repos, use CLI overrides:
```bash
npx tsx src/bin/cli.ts scan --mobile /path/to/mobile --dashboard /path/to/dashboard --platform /path/to/platform
```

Capture the output. Verify:
1. Claude Agent SDK connects successfully
2. Streaming output is visible during scanning
3. No errors during pass execution
4. feature-map.json is created in .sawyer-docs/
5. scan-state.json is created in .sawyer-docs/

If any errors occur, fix them in the relevant source files (passes.ts, scan.ts, prompts.ts) before proceeding to the checkpoint.
  </action>
  <verify>
    <automated>test -f .sawyer-docs/feature-map.json && test -f .sawyer-docs/scan-state.json && echo "PASS: state files created" || echo "FAIL: state files missing"</automated>
  </verify>
  <done>Scan completes without errors, feature-map.json and scan-state.json exist in .sawyer-docs/</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 2: Verify feature map quality and change detection</name>
  <files></files>
  <action>
User reviews the generated feature map for correctness, completeness, and proper classification. Then tests idempotency by running the scan again.
  </action>
  <verify>
    <automated>test -f .sawyer-docs/feature-map.json && echo "PASS" || echo "FAIL"</automated>
  </verify>
  <done>User approves feature map quality — features are correctly classified, infrastructure is excluded, and re-running produces no changes.</done>
  <what-built>Full codebase scanner: multi-pass Claude Code analysis of mobile, dashboard, and platform repos producing a structured feature map with audience classification, admin role levels, and change detection.</what-built>
  <how-to-verify>
    1. Open `.sawyer-docs/feature-map.json` and review the feature list
    2. Verify mobile features have `audience: "end_user"` and `sourceApp: "mobile"`
    3. Verify dashboard features have `audience: "admin"` with `adminRoles` array containing appropriate role levels
    4. Verify no infrastructure components appear (no nav wrappers, loading screens, auth guards)
    5. Verify feature slugs look reasonable (URL-safe, lowercase, hyphenated)
    6. Run the scan again: `npx tsx src/bin/cli.ts scan` — it should report "No changes" and skip re-scanning
    7. Verify `.sawyer-docs/scan-state.json` contains SHA hashes for each scanned repo
  </how-to-verify>
  <resume-signal>Type "approved" if the feature map looks correct and complete, or describe any issues with feature classification, missing features, or incorrect categorization</resume-signal>
</task>

</tasks>

<verification>
```bash
# Validate feature map against schema
npx tsx -e "
import { readFileSync } from 'node:fs';
import { FeatureMapSchema } from './src/scanner/schema.js';
const raw = JSON.parse(readFileSync('.sawyer-docs/feature-map.json', 'utf-8'));
const result = FeatureMapSchema.safeParse(raw);
console.log(result.success ? 'VALID' : result.error.issues);
"

# Idempotency check: second run should skip
npx tsx src/bin/cli.ts scan
```
</verification>

<success_criteria>
- feature-map.json passes FeatureMapSchema validation
- Mobile app features present with end_user audience
- Dashboard features present with admin role classification
- Platform API context attached to relevant features
- No infrastructure components in feature map
- Second scan run skips unchanged repos (change detection working)
- User approves the feature map quality
</success_criteria>

<output>
After completion, create `.planning/phases/02-codebase-scanner/02-03-SUMMARY.md`
</output>
